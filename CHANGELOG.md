# 변경 내역

## 0.2.3 — 2026-09-26

- 컴팩션 상태 파일의 "커밋되지 않은 변경" 목록과 대화 사본의 "바뀐 파일"에서 첫 경로의 첫 글자가 잘리던 문제(`README.md` → `EADME.md`)를 고쳤다. git 출력을 trim하면서 첫 줄 ` M`의 앞 공백이 사라진 탓이다. 상태 목록은 `--porcelain=v1 -z`를 그대로 받아 필드 단위로 읽는다(공백·한글 경로, 이름 바꾸기 안전).
- worklog 판단 칸의 안내를 결론형 "결정 근거"로 바꿨다: 새 이름 `--decision "고른 것 / 버린 대안 / 근거 수치"`, 생각의 흐름·추론 과정은 옮기지 않는다. 옛 이름 `--why`도 계속 받고, 기록 줄 머리 `- 판단:`은 그대로다.
- 응답이 거절·오류로 끝나 Stop 훅이 돌지 않은 턴을 복구한다. 같은 세션에서 이어 보내면 지금처럼 다음 턴에 합치고, 새 세션을 열면 첫 사용자 턴에서 같은 워크트리의 다른 세션 상태를 보고 끝난 것이 분명한 턴(거절·오류로 끝나 1분 넘게 조용, 또는 6시간 넘게 조용)만 최소 항목("원문 미기록")으로 기록한다. 거절을 피하거나 다시 시도하는 장치는 두지 않는다.
- 회귀 시나리오 65.

## 0.2.2 — 2026-09-26

- 대화 글 사본이 Stop 훅에 막힌 턴(worklog 기록 강제·발견 칸 알림)을 두 기록으로 쪼개던 문제를 고쳤다. 막힌 뒤 이어진 Stop(`stop_hook_active` 또는 세션 기록의 "Stop hook feedback" 줄)이고 새 사람 입력이 없으면 직전 기록에 답·worklog 번호·바뀐 파일을 합친다. 도구 색인도 같은 턴 번호로 묶는다. 이미 쪼개진 옛 기록은 고치지 않는다.
- 사람 입력 없이 시작한 턴은 사용자 칸을 빈칸 대신 "(자동 알림)" 또는 "(사용자 입력 없음)"으로 남긴다.
- 회귀 시나리오 64.

## 0.2.1 — 2026-09-26

- 검색 목록의 worklog 번호가 `##66`처럼 #가 두 번 붙던 문제를 고쳤다. `show`는 옛 `##N`도 받는다.
- 검색 목록·`show`·파일별 기억 목록의 시각을 UTC가 아닌 이 컴퓨터의 현지 시각으로 보인다(저장 형식은 ISO UTC 그대로). 컴팩션 상태 파일·요약 머리글은 현지 시각에 시간대를 붙인다.
- "일부 대체됨" 표시만 있는 ADR을 "(대체됨 — 참고만)"으로 내리던 문제를 고쳤다. 전체 대체만 뒤로 보내고, 일부 대체는 따로 표시한다.
- 회귀 시나리오 63.

## 0.2.0 — 2026-09-26

프로젝트 하나에 세션 하나로 오래 일해도 컴팩션 뒤 맥락을 잃지 않고, 지난 결정과 이유를 다시 찾을 수 있게 기억을 넓혔다. 결정: D-프로젝트-기억-분담, D-잊어줘-슬래시명령, D-코드개요-tree-sitter.

- **컴팩션 이어가기**: PreCompact 훅이 마지막 worklog 기록 뒤 구간(사용자 입력 원문·고친 파일·할일·미완료 백그라운드 작업·`/compact` 초점)을 상태 파일로 남기고, 컴팩션 직후 SessionStart(compact)가 상태 파일과 worklog를 "컴팩션 전 기록"으로 넣는다. PostCompact는 요약을 저장만 한다. CLAUDE.md에 `Compact instructions`(이유·조건·수치·미결 질문을 남기라) 절. 효과 측정용으로 `ORBIT_COMPACT_WORKLOG=off`면 컴팩션 직후 worklog를 빼고 상태 파일만 넣는다.
- **worklog**: `recent`가 최근 8턴과 그 앞 구간 요약을 넣는다(최근 턴과 겹치는 요약·알림 묶음 제외). 선택 칸 `--why`(판단)·`--found`(발견)와 이미 쓴 항목에 발견을 덧붙이는 `found`. 작성자에 다른 에이전트 이름 허용. 도구를 많이 쓴 턴에 발견 칸이 비면 종료 훅이 한 번 알린다. Claude가 이미 기록한 알림은 다시 묶지 않는다. 훅 배선이 오류를 숨기지 않는다.
- **대화 글 사본·도구 활동 색인**: 턴이 끝날 때 사람·AI 글(가림)과 도구 활동(이름·파일·명령 앞부분)을 `.git` 공용 폴더에 쌓는다(앞으로의 대화만, 자동 삭제 없음, 바뀐 파일은 git 상태로 추정).
- **찾기**: `memory.mjs search`(ADR·위키·worklog·대화 사본·도구 활동, 번호 목록)와 `show <번호>`. `/recall` 스킬과 Sonnet 서브에이전트 `recall-searcher`가 뜻으로 깊게 찾는다. 파일을 읽거나 고칠 때 그 파일의 과거 턴 목록을 넣는다.
- **지우기**: `/forget <문구>` 슬래시 스킬(모델이 스스로 부르지 않음, 미리 보기→확인→적용).
- **코드 개요·펼치기**: `code.mjs outline|unfold`(tree-sitter WASM, 설치 때 받음). 큰 코드 파일을 범위 없이 읽으면 개요와 부분 읽기 안내를 넣는다.
- **설치기**: CLAUDE.md를 orbit 관리 구간과 "프로젝트 메모"로 나눠 update가 구간만 바꾼다. codex를 끈 설치는 AGENTS.md를 가리키지 않는다. 매니페스트에 실제 플러그인 판을 기록한다(예전엔 0.0.0).
- 새 요구: 설치·갱신 때 npm과 네트워크(코드 도구). 받지 못해도 나머지는 설치하고 알린다.
- 회귀 시나리오 44~62.

## 0.1.7 — 2026-09-24

- 저장소를 새로 만들어 이력을 이 버전부터 다시 시작했다. 이전에 복제했거나 마켓플레이스로 등록했다면 다시 등록한다(`/plugin marketplace remove orbit` 뒤 `/plugin marketplace add vriznet/orbit`).
- ECC에서 가져온 리뷰어 에이전트 5종과 `/aside`·`/checkpoint`의 원 저작권·라이선스 고지를 `THIRD_PARTY_NOTICES.md`에 담았다.

## 0.1.6 — 2026-09-24

- 에이전트 정책 훅이 orbit이 설치된 저장소(`.claude/orbit-manifest.json`이 있는 저장소)에서만 general-purpose·fork 위임을 거부한다. 플러그인 훅은 모든 프로젝트에서 돌기 때문에, 다른 프로젝트의 위임에는 끼어들지 않게 했다. 회귀 시나리오 43 추가.

## 0.1.5 — 2026-09-24

- 설치기가 실행 부산물(`__pycache__`, `*.pyc`, `.DS_Store`)을 자산으로 복사하지 않는다.

## 0.1.4 — 2026-09-24

- **자동 학습(ECC) 선택 모듈을 뺐다.** 설치 질문과 `--ecc` 옵션이 사라졌다. 이미 켠 저장소는 `update` 때 자동 학습 훅 연결이 해제되고, 남은 파일(`.claude/skills/continuous-learning-v2`, `.claude/rules/ecc`, 학습 명령)은 필요 없으면 지워도 된다.
- **필수 도구 확인**: `/orbit:setup`·`/orbit:update`가 git과 Node.js 18 이상을 먼저 확인한다. 없으면 설치를 안내하고, 사용자가 설치를 거부하면 설치를 취소한다. macOS에서는 명령줄 도구 설치 창이 저절로 뜨지 않게 확인한다.
- 설치기가 모르는 옵션을 거부하고, 값 옵션 뒤에 값 대신 다른 플래그가 오면 멈춘다.
- README 요구사항에 git·Node.js를 적었다.

## 0.1.3 — 2026-09-24

- **알림 턴 처리**: 백그라운드 작업 완료 알림(`<task-notification>`)을 worklog 사용자 턴으로 세지 않는다. 알림 턴에는 기록 지시를 내보내지 않고 Stop 훅도 차단하지 않는다. 쉬는 동안 온 알림은 다음 사용자 턴 직전에 `(자동 알림 N건)` 한 항목으로 묶어 기록하고(20건이 쌓이거나 10분이 지나면 먼저 기록), 사용자 턴 도중 온 알림은 그 턴에 개수만 붙인다. 판별은 좁게 한다 — 알림 블록 밖에 글이 섞이면 사용자 턴이다.
- **진행 중 추가 입력 합치기**: 같은 `prompt_id`로 들어온 입력(작업 중 보낸 추가 메시지, 한 도착분에 begin이 여러 번 도는 경우)은 새 턴을 열지 않는다. 중단 뒤 다시 보낸 메시지는 앞 턴을 대기열로 밀지 않고 새 턴에 합친다. 옛 토큰만으로는 새 턴이 닫히지 않는다.
- **자동 복구 문구**: 원인(종료 차단 후에도 미기록 / 자동 복구 실패 / 이전 버전 훅)을 구분해 적는다.
- 턴 상태 파일에 잠금을 걸어 동시에 도착한 입력이 서로의 갱신을 덮어쓰지 않게 했다.
- 진단용 입력 덤프: `ORBIT_HOOK_DEBUG=1`이거나 `.git/orbit-state/worklog/debug` 파일이 있으면 원문 없이 태그 뼈대만 남긴다.
- 회귀 시나리오 41 추가.

## 0.1.2 — 2026-09-22

- `update`가 옛 `.git/harness-state/worklog` 턴 상태를 `orbit-state`로 옮긴다(회귀 시나리오 40).

## 0.1.1 — 2026-09-22

- full-access를 뺀 위임 모드 7종에서 `Agent` 도구를 막아 하위 위임으로 접근 제한을 우회하지 못하게 했다.

# orbit:update (skill)

orbit 플러그인이 배포하는 갱신 스킬. orbit:setup의 `install.mjs update`를 호출하는 얇은 companion이다.

- 전제: 대상 저장소에 `.claude/orbit-manifest.json`(orbit:setup으로 설치된 흔적).
- 로직은 여기 없다 — 형제 디렉터리 `../setup/scripts/install.mjs`가 전부 담당한다.
